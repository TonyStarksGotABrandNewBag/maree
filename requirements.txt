# Versions match Sir's working .venv. The earlier scaffold pins (numpy
# 2.1.3 + catboost 1.2.7) were a stale combo — catboost 1.2.7 caps numpy
# at <2.0, which CI's strict resolver (correctly) rejects.

# Core data + ML
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1

# Gradient boosting (advanced baselines + the LightGBM M.A.R.E.E. variant)
xgboost==3.2.0
lightgbm==4.6.0
catboost==1.2.10

# NOTE: torch is intentionally NOT in production requirements. It's lazy-
# imported by src.models.baselines and only used by the Phase-D MLP baseline
# test (tests/test_models_torch.py). Keeping it out saves ~800 MB of wheel
# weight and avoids OOM during pip wheel resolution on free-tier build
# hosts. See requirements-test.txt — installed only by the test-torch CI job.

# Web application
flask==3.1.3
gunicorn==25.3.0

# LLM triage layer (optional at runtime — falls back to deterministic
# template backend when ANTHROPIC_API_KEY is unset)
anthropic==0.97.0

# Testing + lint
pytest==9.0.3
ruff==0.8.0
